



class ParentWindow(Toon):
    def __init__ ():
        Toon.__init__

        name = kAPP
        race = android
        class_type = mechanic
        drone = murco

def present(self):
        intro = ("{} is an {} {} who works with his drone {} to scout and lock down enemies".format(name,race,class_type,drone))
        print(intro)


class drone(drone):
        chasis = hover
        use = surveillance

self.present
